import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    row: { flexDirection: 'column', justifyContent: 'space-between', marginTop: 8 },
    happinessScore: {
        fontSize: 16,
        fontWeight: "500"
    },
    roomName: {
        color: "#999"
    },
});
